import React, { useEffect } from "react";
import styles from "./Logout.module.scss";

const Logout = (props: any) => {
  return <div className={styles.logout}>Logout Component</div>;
};
export default Logout;
